# 申し送り 2026-04-12

## やったこと
- AI組織体制設計完了（CLAUDE.md、コンテンツ部・マーケティング部SOP）
- GitHub Actions作成（週次ブログ・SNS自動生成）
- setup.py作成（python3で全ファイル一括作成）

## 次にやること
1. ~~pnpm install && pnpm add @anthropic-ai/sdk tsx~~ → `package.json` に `@anthropic-ai/sdk` と `generate-post` を追加済み。ローカルは `npm install` / CI は `pnpm install` で取得。
2. ~~package.jsonにgenerate-postスクリプト追加~~ → `npm run generate-post`（引数: `--keyword`, `--words`）
3. GitHub Secretsに `ANTHROPIC_API_KEY` 設定（リポジトリ Settings → Secrets）
4. shaho-life.comドメインをVercelに設定
5. microCMS 連携と AI 記事投稿フロー統合（現状はファイル生成 → PR。CMS 投稿は別途）

## 環境
- shaho-website: /Users/user/Documents/shaho-website
- shaho-web: /Users/user/Documents/shaho-web
- CI: GitHub Actions は `pnpm/action-setup` 利用。ローカルは npm でも可。
- Python: pyenv 3.10.13
